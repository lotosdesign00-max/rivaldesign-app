# 🚀 Деплой на Vercel с Amplitude

## Настройка переменных окружения на Vercel

### Шаг 1: Получите API ключ Amplitude
1. Зайдите на https://analytics.amplitude.com/
2. Settings → Projects → [Your Project] → API Keys
3. Скопируйте **API Key**

### Шаг 2: Добавьте переменную на Vercel

#### Вариант А: Через веб-интерфейс
1. Откройте https://vercel.com/
2. Выберите ваш проект
3. Перейдите в **Settings → Environment Variables**
4. Добавьте новую переменную:
   - **Name:** `VITE_AMPLITUDE_API_KEY`
   - **Value:** ваш_API_ключ_от_Amplitude
   - **Environment:** Выберите все (Production, Preview, Development)
5. Нажмите **Save**

#### Вариант Б: Через Vercel CLI
```bash
vercel env add VITE_AMPLITUDE_API_KEY
# Вставьте ваш API ключ когда попросит
# Выберите: Production, Preview, Development (все)
```

### Шаг 3: Передеплойте проект
После добавления переменной нужно пересобрать проект:

1. В Vercel Dashboard → **Deployments**
2. Найдите последний деплой → нажмите **⋯** → **Redeploy**
3. Или просто сделайте новый push в GitHub:
```bash
git commit --allow-empty -m "redeploy: Update environment variables"
git push origin master
```

### Шаг 4: Проверьте работу
1. Откройте ваше приложение на Vercel
2. Откройте консоль браузера (F12)
3. Должны увидеть:
   - `[Amplitude] ✅ Analytics initialized (Production)`
   - `[Amplitude] 📊 Event: app_start`

4. Зайдите на https://analytics.amplitude.com/
5. Перейдите в **Live View** - увидите события в реальном времени!

---

## 📊 Что будет отслеживаться в продакшене

Все события из `src/utils/analytics.js`:
- ✅ Запуск приложения (app_start)
- ✅ Источник трафика (traffic_source) 
- ✅ Просмотры разделов (section_view)
- ✅ Воронка покупки (view_gallery → view_pricing → select_plan)
- ✅ Взаимодействия с галереей
- ✅ Генерация идей
- ✅ Клики по кнопкам

**В продакшене логи в консоль НЕ выводятся** (только на localhost для отладки).

---

## ⚠️ Важно

1. **Не коммитьте .env файл!** Он уже в .gitignore
2. API ключ должен быть добавлен **только в Vercel Environment Variables**
3. После добавления переменной - обязательно redeploy!

---

## 🔍 Проверка текущего деплоя

Чтобы убедиться, что переменная загружена:
```bash
vercel env ls
```

Должно показать `VITE_AMPLITUDE_API_KEY` для всех сред.

---

✅ **Готово!** Теперь статистика будет собираться с реальных пользователей на Vercel!
