# 🚀 Автоматическая интеграция Google Drive

## Этот метод автоматически загружает список файлов из ваших папок на Google Drive!

---

## Шаг 1: Получите Google API ключ (5 минут)

### 1.1. Создайте проект в Google Cloud Console

1. Перейдите на [Google Cloud Console](https://console.cloud.google.com/)
2. Нажмите "Select a project" → "New Project"
3. Введите название (например: "Design Pack App")
4. Нажмите "Create"

### 1.2. Включите Google Drive API

1. В меню слева выберите "APIs & Services" → "Library"
2. Найдите "Google Drive API"
3. Нажмите на него и нажмите "Enable"

### 1.3. Создайте API ключ

1. Перейдите в "APIs & Services" → "Credentials"
2. Нажмите "Create Credentials" → "API Key"
3. **Скопируйте ключ** (важно!)
4. Нажмите "Edit API key" для настройки ограничений:
   - **Application restrictions**: выберите "HTTP referrers"
   - Добавьте `http://localhost:*` и ваш домен
   - **API restrictions**: выберите "Restrict key"
   - Выберите только "Google Drive API"
5. Нажмите "Save"

---

## Шаг 2: Подготовьте файлы на Google Drive (3 минуты)

### 2.1. Структура папок

Создайте на Google Drive структуру:

```
📁 Design Pack/
  ├── 📁 Fonts/
  │   ├── font1.ttf
  │   ├── font2.otf
  │   └── ...
  ├── 📁 Textures/
  │   ├── texture1.jpg
  │   ├── texture2.png
  │   └── ...
  ├── 📁 Brushes/
  │   ├── brush1.abr
  │   └── ...
  └── 📁 Icons/
      ├── icons1.zip
      └── ...
```

### 2.2. Сделайте папки публичными

**Для каждой папки:**
1. Правый клик → "Share"
2. "General access" → **"Anyone with the link"**
3. Role: **"Viewer"**
4. Нажмите "Done"

### 2.3. Получите ID папок

Откройте каждую папку и посмотрите на URL:

```
https://drive.google.com/drive/folders/1ABC123xyz_FOLDER_ID_HERE
```

**ID папки** = `1ABC123xyz_FOLDER_ID_HERE`

Скопируйте ID для каждой папки (Fonts, Textures, Brushes, Icons).

---

## Шаг 3: Настройте приложение (2 минуты)

### 3.1. Создайте .env файл

Создайте файл `.env` в корне проекта:

```env
VITE_GOOGLE_API_KEY=AIza...ваш_api_ключ_здесь
```

### 3.2. Обновите src/config/driveFolders.js

Откройте файл `src/config/driveFolders.js` и замените ID:

```javascript
export const DRIVE_FOLDER_IDS = {
  ru: {
    "Шрифты": "1ABC_ID_ПАПКИ_FONTS",
    "Текстуры": "1XYZ_ID_ПАПКИ_TEXTURES",
    "Кисти": "1QWE_ID_ПАПКИ_BRUSHES",
    "Иконки": "1ZXC_ID_ПАПКИ_ICONS"
  },
  en: {
    "Fonts": "1ABC_ID_ПАПКИ_FONTS",
    "Textures": "1XYZ_ID_ПАПКИ_TEXTURES",
    "Brushes": "1QWE_ID_ПАПКИ_BRUSHES",
    "Icons": "1ZXC_ID_ПАПКИ_ICONS"
  }
};
```

### 3.3. Обновите App.jsx для загрузки из Google Drive

Добавьте в начало компонента `FreePackTab`:

```javascript
import { getMaterialsFromDrive } from '../utils/googleDrive';
import { DRIVE_FOLDER_IDS } from '../config/driveFolders';

// Внутри компонента FreePackTab
const [materials, setMaterials] = useState([]);
const [loading, setLoading] = useState(true);

useEffect(() => {
  async function loadMaterials() {
    setLoading(true);
    const folderIds = DRIVE_FOLDER_IDS[lang] || DRIVE_FOLDER_IDS.ru;
    const materialsFromDrive = await getMaterialsFromDrive(folderIds);
    setMaterials(materialsFromDrive);
    setLoading(false);
  }
  
  if (isSubscribed) {
    loadMaterials();
  }
}, [isSubscribed, lang]);
```

---

## Шаг 4: Запустите приложение

```bash
npm run dev
```

Откройте http://localhost:5173/

---

## ✅ Готово!

Теперь приложение автоматически:
1. ✅ Загружает список всех файлов из ваших папок на Google Drive
2. ✅ Показывает их в приложении с категориями
3. ✅ Позволяет скачивать каждый файл отдельно

---

## 🔧 Альтернатива: Одна общая папка

Если все файлы в одной папке (не разделены по категориям):

1. Получите ID главной папки
2. В `src/config/driveFolders.js` используйте:

```javascript
export const MAIN_FOLDER_ID = "1ABC_ID_ВАШЕЙ_ПАПКИ";
```

3. Файлы будут автоматически распределены по категориям по расширению:
   - `.ttf`, `.otf`, `.woff` → Шрифты
   - `.jpg`, `.png`, `.jpeg` → Текстуры
   - `.abr`, `.brush` → Кисти
   - `.svg`, `.ai`, `.eps` → Иконки

---

## 📝 Примечания

- **API квота**: 1000 запросов в день (бесплатно)
- **Кэширование**: Список файлов загружается один раз при открытии вкладки
- **Превью**: Автоматически генерируются Google Drive
- **Безопасность**: API ключ работает только с вашего домена

---

## 🆘 Проблемы?

### Ошибка "API key not valid"
- Проверьте что API ключ скопирован правильно в `.env`
- Убедитесь что Google Drive API включен в проекте

### Файлы не загружаются
- Проверьте что папки публичные (Anyone with the link)
- Проверьте что ID папок правильные
- Откройте консоль браузера (F12) для деталей ошибки

### Превью не показываются
- Превью генерируются автоматически Google Drive
- Для некоторых форматов превью может не быть
