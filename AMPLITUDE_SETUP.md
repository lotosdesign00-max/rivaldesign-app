# 📊 Настройка Amplitude Analytics

## Быстрый старт

### 1. Получите API ключ
1. Зайдите на https://analytics.amplitude.com/
2. Войдите в свой аккаунт (или создайте новый)
3. Перейдите в **Settings → Projects → [Your Project] → API Keys**
4. Скопируйте **API Key**

### 2. Добавьте ключ в проект
Откройте файл `.env` и замените `YOUR_AMPLITUDE_API_KEY` на ваш реальный ключ:

```env
VITE_AMPLITUDE_API_KEY=ваш_настоящий_ключ_здесь
```

**⚠️ ВАЖНО:** Файл `.env` уже добавлен в `.gitignore` и НЕ будет загружен на GitHub!

### 3. Запустите проект
```bash
npm install
npm run dev
```

Откройте консоль браузера - вы увидите сообщения от Amplitude:
- `[Amplitude] ✅ Analytics initialized (Dev Mode)`
- `[Amplitude] 📊 Event: app_start`

### 4. Проверьте статистику
Зайдите на https://analytics.amplitude.com/ и выберите свой проект. 
События начнут появляться в реальном времени!

---

## 📈 Какие события отслеживаются

| Событие | Описание | Когда срабатывает |
|---------|----------|-------------------|
| `app_start` | Запуск приложения | При первой загрузке |
| `traffic_source` | Источник трафика | Если есть параметр запуска |
| `section_view` | Просмотр раздела | При переключении табов |
| `view_gallery` | Просмотр галереи | Открытие вкладки Gallery |
| `view_pricing` | Просмотр цен | Открытие вкладки Pricing |
| `select_plan` | Выбор тарифа | Добавление в корзину |
| `button_click` | Клик по кнопке | Различные действия |
| `gallery_interaction` | Взаимодействие с галереей | Открытие/закрытие изображений |
| `generate_idea` | Генерация идеи | Использование генератора идей |
| `user_activity` | Активность пользователя | Периодически |

---

## 🔧 Как добавить новые события

### Пример 1: Простое событие
```javascript
import { trackCustomEvent } from './utils/analytics';

trackCustomEvent('new_event_name', {
  property1: 'value1',
  property2: 'value2'
});
```

### Пример 2: Использование готовых функций
```javascript
import { trackButtonClick } from './utils/analytics';

trackButtonClick('subscribe_button', 'homepage');
```

---

## 🚀 Продакшен

В продакшене убедитесь, что:
1. В `.env` указан правильный API ключ
2. Домен не `localhost` (иначе будет пометка `environment: development`)
3. События будут отправляться автоматически

---

## 📊 Дашборды Amplitude

Рекомендуемые метрики для отслеживания:
- **DAU/MAU** - активные пользователи
- **Retention** - возвращаемость пользователей
- **Conversion Funnel** - воронка конверсии (app_start → view_pricing → select_plan)
- **User Journey** - путь пользователя по разделам

---

## ❓ Проблемы

### Не видите события?
1. Проверьте консоль браузера - должны быть логи `[Amplitude] 📊 Event: ...`
2. Убедитесь, что API ключ правильный в `.env`
3. Перезапустите dev сервер: `npm run dev`

### События не появляются на сайте Amplitude?
1. Подождите 1-2 минуты (небольшая задержка нормальна)
2. Проверьте, что выбран правильный проект
3. Используйте Live View для мониторинга в реальном времени

---

Создано для **Rival Space Telegram Mini App** 🚀
