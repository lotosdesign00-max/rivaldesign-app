import React, { useEffect, useMemo, useState } from "react";
import SystemIcon from "../legacy/components/SystemIcon.jsx";

const LOCAL_ADMIN_ID = "999999001";
const NAV_ITEMS = [
  { id: "dashboard", label: "РћР±Р·РѕСЂ", icon: "spark" },
  { id: "inbox", label: "Inbox", icon: "chat" },
  { id: "payments", label: "РџР»Р°С‚РµР¶Рё", icon: "wallet" },
  { id: "clients", label: "РљР»РёРµРЅС‚С‹", icon: "users" },
  { id: "portfolio", label: "РџРѕСЂС‚С„РѕР»РёРѕ", icon: "gallery" },
  { id: "courses", label: "РљСѓСЂСЃС‹", icon: "book" },
  { id: "services", label: "РЈСЃР»СѓРіРё", icon: "grid" },
  { id: "reviews", label: "РћС‚Р·С‹РІС‹", icon: "star" },
  { id: "faq", label: "FAQ", icon: "info" },
  { id: "home", label: "Р“Р»Р°РІРЅР°СЏ", icon: "home" },
  { id: "socials", label: "РЎРѕС†СЃРµС‚Рё", icon: "globe" },
];

const ORDER_STATUS_OPTIONS = [
  ["waiting_payment", "РћР¶РёРґР°РµС‚ РѕРїР»Р°С‚Сѓ"],
  ["payment_review", "РџСЂРѕРІРµСЂРєР° РѕРїР»Р°С‚С‹"],
  ["queued", "Р’ РѕС‡РµСЂРµРґРё"],
  ["in_progress", "Р’ СЂР°Р±РѕС‚Рµ"],
  ["preview_sent", "РџСЂРµРІСЊСЋ РѕС‚РїСЂР°РІР»РµРЅРѕ"],
  ["revision", "РџСЂР°РІРєРё"],
  ["delivered", "Р“РѕС‚РѕРІРѕ"],
  ["closed", "Р—Р°РєСЂС‹С‚"],
  ["canceled", "РћС‚РјРµРЅРµРЅ"],
];

const PAYMENT_STATUS_OPTIONS = [
  ["pending", "РћР¶РёРґР°РµС‚"],
  ["paid", "РћРїР»Р°С‡РµРЅ"],
  ["canceled", "РћС‚РјРµРЅРµРЅ"],
  ["refunded", "Р’РѕР·РІСЂР°С‚"],
];

const contentConfig = {
  portfolio: { title: "РџРѕСЂС‚С„РѕР»РёРѕ", path: ["gallery", "ru"], template: { id: `g_${Date.now()}`, cat: "РџСЂРµРІСЊСЋ", title: "РќРѕРІР°СЏ СЂР°Р±РѕС‚Р°", desc: "РљРѕСЂРѕС‚РєРѕРµ РѕРїРёСЃР°РЅРёРµ", img: "/images/new-work.jpg", tags: ["new"], popular: false, views: 0 }, getLabel: (item) => item?.title || item?.id || "Р Р°Р±РѕС‚Р°", getMeta: (item) => item?.cat || "Р‘РµР· РєР°С‚РµРіРѕСЂРёРё" },
  courses: { title: "РљСѓСЂСЃС‹", path: ["courses", "ru"], template: { id: `c_${Date.now()}`, cat: "РќРѕРІС‹Р№", title: "РќРѕРІС‹Р№ РєСѓСЂСЃ", desc: "РћРїРёСЃР°РЅРёРµ РєСѓСЂСЃР°", level: "РЎСЂРµРґРЅРёР№", duration: "3 С‡", lessons: 6, img: "/images/course-cover.jpg", popular: false, free: false, price: 10, rating: 5, students: 0, topics: ["РўРµРјР° 1", "РўРµРјР° 2"] }, getLabel: (item) => item?.title || item?.id || "РљСѓСЂСЃ", getMeta: (item) => `${item?.price ?? 0}$ В· ${item?.lessons ?? 0} СѓСЂРѕРєРѕРІ` },
  services: { title: "РЈСЃР»СѓРіРё", path: ["services"], template: { id: Date.now(), icon: "вњ¦", key: `service_${Date.now()}`, priceUSD: 10, ru: "РќРѕРІР°СЏ СѓСЃР»СѓРіР°", en: "New service", ua: "РќРѕРІР° РїРѕСЃР»СѓРіР°", kz: "Р–Р°ТЈР° Т›С‹Р·РјРµС‚", by: "РќРѕРІР°СЏ РїР°СЃР»СѓРіР°", descRu: "РћРїРёСЃР°РЅРёРµ СѓСЃР»СѓРіРё", descEn: "Service description", timeRu: "1вЂ“2 РґРЅСЏ", timeEn: "1вЂ“2 days", features: ["РћРїС†РёСЏ 1", "РћРїС†РёСЏ 2"] }, getLabel: (item) => item?.ru || item?.en || item?.key || "РЈСЃР»СѓРіР°", getMeta: (item) => `$${item?.priceUSD ?? 0}` },
  reviews: { title: "РћС‚Р·С‹РІС‹", path: ["reviews"], template: { id: `r_${Date.now()}`, name: "РќРѕРІС‹Р№ РєР»РёРµРЅС‚", tg: "username", rating: 5, text: "РўРµРєСЃС‚ РѕС‚Р·С‹РІР°", time: "СЃРµРіРѕРґРЅСЏ", verified: true }, getLabel: (item) => item?.name || item?.id || "РћС‚Р·С‹РІ", getMeta: (item) => `${item?.rating ?? 0}/5` },
  faq: { title: "FAQ", path: ["faq", "ru"], template: { q: "РќРѕРІС‹Р№ РІРѕРїСЂРѕСЃ?", a: "РќРѕРІС‹Р№ РѕС‚РІРµС‚." }, getLabel: (item) => item?.q || "Р’РѕРїСЂРѕСЃ", getMeta: () => "FAQ" },
  home: { title: "Р“Р»Р°РІРЅР°СЏ", path: ["home", "stats"], template: { value: "0", labelRu: "РќРѕРІС‹Р№ РїСѓРЅРєС‚", labelEn: "New stat" }, getLabel: (item) => item?.labelRu || item?.labelEn || "РЎС‚Р°С‚РёСЃС‚РёРєР°", getMeta: (item) => item?.value || "0" },
  socials: { title: "РЎРѕС†СЃРµС‚Рё", path: ["home", "socials"], template: { kind: "telegram", label: "Telegram", url: "https://t.me/username", accent: "#229ED9" }, getLabel: (item) => item?.label || item?.kind || "РЎРѕС†СЃРµС‚СЊ", getMeta: (item) => item?.url || "" },
};

const styles = {
  bg: { minHeight: "100vh", background: "radial-gradient(circle at top left, rgba(96,104,255,.18), transparent 24%), radial-gradient(circle at top right, rgba(56,189,248,.12), transparent 22%), linear-gradient(180deg, #05070b 0%, #090b12 100%)", color: "rgba(244,244,245,.96)" },
  shell: { display: "grid", gridTemplateColumns: "280px minmax(0,1fr)", gap: 18, padding: 18 },
  card: { border: "1px solid rgba(255,255,255,.08)", background: "linear-gradient(180deg, rgba(17,19,28,.92) 0%, rgba(10,12,18,.96) 100%)", borderRadius: 28, boxShadow: "0 18px 56px rgba(0,0,0,.36), inset 0 1px 0 rgba(255,255,255,.04)", backdropFilter: "blur(18px)" },
};

function money(value) { return `$${Number(value || 0).toFixed(2)}`; }
function formatDate(value) { if (!value) return "вЂ”"; const date = new Date(value); return date.toLocaleString("ru-RU", { day: "2-digit", month: "2-digit", hour: "2-digit", minute: "2-digit" }); }
function getPath(obj, path) { return path.reduce((acc, key) => acc?.[key], obj); }
function setPath(obj, path, value) { const clone = structuredClone(obj); let cursor = clone; for (let i = 0; i < path.length - 1; i += 1) { const key = path[i]; if (!cursor[key]) cursor[key] = typeof path[i + 1] === "number" ? [] : {}; cursor = cursor[key]; } cursor[path[path.length - 1]] = value; return clone; }
function buildTelegramUser(rawUser, devId) { if (rawUser?.id) return rawUser; const numericId = Number(devId || LOCAL_ADMIN_ID); if (!numericId) return null; return { id: numericId, username: "local_admin", first_name: "Local", last_name: "Admin" }; }
async function adminPost(path, payload, telegramUser) { const response = await fetch(path, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ...payload, telegramUser }) }); const json = await response.json().catch(() => null); if (!response.ok || !json?.ok) throw new Error(json?.error || "Admin request failed"); return json.result; }

function StatCard({ icon, label, value, sub }) {
  return <div style={{ ...styles.card, padding: 18, display: "flex", flexDirection: "column", gap: 10 }}><div style={{ width: 46, height: 46, borderRadius: 16, display: "grid", placeItems: "center", background: "linear-gradient(135deg, rgba(99,102,241,.22), rgba(56,189,248,.18))", border: "1px solid rgba(255,255,255,.08)" }}><SystemIcon name={icon} size={20} color="#fff" animated /></div><div style={{ fontSize: 11, textTransform: "uppercase", letterSpacing: ".12em", color: "rgba(161,161,170,.8)" }}>{label}</div><div style={{ fontSize: 30, fontWeight: 900, lineHeight: 1, letterSpacing: "-.04em" }}>{value}</div>{sub ? <div style={{ fontSize: 12, color: "rgba(161,161,170,.82)" }}>{sub}</div> : null}</div>;
}

function Sidebar({ section, setSection, dirtyContent }) {
  return <aside style={{ ...styles.card, padding: 18, position: "sticky", top: 18, height: "calc(100vh - 36px)", overflow: "auto" }}><div style={{ display: "flex", alignItems: "center", gap: 12, marginBottom: 20 }}><div style={{ width: 52, height: 52, borderRadius: 18, display: "grid", placeItems: "center", background: "linear-gradient(135deg, rgba(255,255,255,.12), rgba(255,255,255,.04))", border: "1px solid rgba(255,255,255,.1)" }}><SystemIcon name="spark" size={22} color="#fff" animated /></div><div><div style={{ fontSize: 22, fontWeight: 900, letterSpacing: "-.04em" }}>Rival Admin</div><div style={{ fontSize: 12, color: "rgba(161,161,170,.82)" }}>РѕРїРµСЂР°С†РёРѕРЅРєР° + РєРѕРЅС‚РµРЅС‚</div></div></div><div style={{ display: "grid", gap: 8 }}>{NAV_ITEMS.map((item) => { const active = section === item.id; return <button key={item.id} onClick={() => setSection(item.id)} style={{ display: "flex", alignItems: "center", gap: 12, width: "100%", padding: "13px 14px", borderRadius: 16, border: `1px solid ${active ? "rgba(99,102,241,.36)" : "rgba(255,255,255,.06)"}`, background: active ? "linear-gradient(135deg, rgba(99,102,241,.18), rgba(56,189,248,.10))" : "rgba(255,255,255,.03)", color: "rgba(244,244,245,.96)", cursor: "pointer", textAlign: "left" }}><span style={{ width: 26, display: "grid", placeItems: "center" }}><SystemIcon name={item.icon} size={18} color={active ? "#fff" : "rgba(212,212,216,.9)"} animated /></span><span style={{ flex: 1, fontWeight: 700 }}>{item.label}</span>{dirtyContent && ["portfolio", "courses", "services", "reviews", "faq", "home", "socials"].includes(item.id) ? <span style={{ width: 8, height: 8, borderRadius: 999, background: "#f59e0b" }} /> : null}</button>; })}</div></aside>;
}
function Dashboard({ metrics, orders, payments }) {
  const latestOrders = orders.slice(0, 5);
  const latestPayments = payments.slice(0, 5);
  return <div style={{ display: "grid", gap: 18 }}><div style={{ display: "grid", gridTemplateColumns: "repeat(4, minmax(0,1fr))", gap: 14 }}><StatCard icon="clipboard" label="РђРєС‚РёРІРЅС‹Рµ" value={metrics.activeOrders} sub="Р¶РёРІС‹Рµ РїСЂРѕРµРєС‚С‹" /><StatCard icon="wallet" label="Р’С‹СЂСѓС‡РєР°" value={money(metrics.revenue)} sub="РѕРїР»Р°С‡РµРЅРЅС‹Рµ РїР»Р°С‚РµР¶Рё" /><StatCard icon="chat" label="Inbox" value={metrics.inbox} sub="Р·Р°РїСЂРѕСЃС‹ СЂРµРєРІРёР·РёС‚РѕРІ" /><StatCard icon="users" label="РљР»РёРµРЅС‚С‹" value={metrics.users} sub="РІ Р±Р°Р·Рµ Supabase" /></div><div style={{ display: "grid", gridTemplateColumns: "1.2fr .8fr", gap: 18 }}><div style={{ ...styles.card, padding: 18 }}><div style={{ fontSize: 18, fontWeight: 900, marginBottom: 14 }}>РЎРІРµР¶РёРµ Р·Р°РєР°Р·С‹</div><div style={{ display: "grid", gap: 10 }}>{latestOrders.map((order) => <div key={order.id} style={{ padding: 14, borderRadius: 16, border: "1px solid rgba(255,255,255,.06)", background: "rgba(255,255,255,.03)", display: "flex", justifyContent: "space-between", gap: 12 }}><div><div style={{ fontSize: 11, color: "rgba(161,161,170,.8)", marginBottom: 4 }}>{order.order_number}</div><div style={{ fontSize: 15, fontWeight: 800 }}>{order.service_name}</div><div style={{ fontSize: 12, color: "rgba(161,161,170,.82)", marginTop: 4 }}>{order.users?.username || order.users?.first_name || "РљР»РёРµРЅС‚"}</div></div><div style={{ textAlign: "right" }}><div style={{ fontWeight: 900 }}>{money(order.total_amount)}</div><div style={{ fontSize: 12, color: "rgba(161,161,170,.82)", marginTop: 4 }}>{order.status}</div></div></div>)}</div></div><div style={{ ...styles.card, padding: 18 }}><div style={{ fontSize: 18, fontWeight: 900, marginBottom: 14 }}>РџР»Р°С‚РµР¶РЅС‹Р№ РїРѕС‚РѕРє</div><div style={{ display: "grid", gap: 10 }}>{latestPayments.map((payment) => <div key={payment.id} style={{ padding: 14, borderRadius: 16, border: "1px solid rgba(255,255,255,.06)", background: "rgba(255,255,255,.03)", display: "flex", justifyContent: "space-between", gap: 12 }}><div><div style={{ fontWeight: 800 }}>{payment.users?.username || payment.users?.first_name || "РљР»РёРµРЅС‚"}</div><div style={{ fontSize: 12, color: "rgba(161,161,170,.82)", marginTop: 4 }}>{payment.payment_method}</div></div><div style={{ textAlign: "right" }}><div style={{ fontWeight: 900 }}>{money(payment.amount)}</div><div style={{ fontSize: 12, color: payment.status === "paid" ? "#34d399" : "#fbbf24", marginTop: 4 }}>{payment.status}</div></div></div>)}</div></div></div></div>;
}

function Payments({ payments, onUpdate, onOpenOrder }) {
  return <div style={{ ...styles.card, padding: 18, display: "grid", gap: 10 }}>{payments.map((payment) => <div key={payment.id} style={{ padding: 14, borderRadius: 18, border: "1px solid rgba(255,255,255,.06)", background: "rgba(255,255,255,.03)", display: "grid", gridTemplateColumns: "1.1fr .8fr .7fr auto", alignItems: "center", gap: 12 }}><div><div style={{ fontWeight: 800 }}>{payment.users?.username || payment.users?.first_name || "РљР»РёРµРЅС‚"}</div><div style={{ fontSize: 12, color: "rgba(161,161,170,.82)", marginTop: 4 }}>{payment.payment_method} В· {formatDate(payment.created_at)}</div></div><div style={{ fontWeight: 900 }}>{money(payment.amount)}</div><select value={payment.status} onChange={(e) => onUpdate(payment.id, e.target.value)} style={{ height: 42, borderRadius: 12, border: "1px solid rgba(255,255,255,.08)", background: "rgba(255,255,255,.04)", color: "#fff", padding: "0 12px" }}>{PAYMENT_STATUS_OPTIONS.map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select><button onClick={() => onOpenOrder(payment.id)} style={{ height: 42, padding: "0 14px", borderRadius: 12, border: "1px solid rgba(255,255,255,.08)", background: "rgba(255,255,255,.05)", color: "#fff", cursor: "pointer" }}>РћС‚РєСЂС‹С‚СЊ</button></div>)}</div>;
}

function Clients({ users, onUpdateBalance }) {
  const [drafts, setDrafts] = useState({});
  return <div style={{ ...styles.card, padding: 18, display: "grid", gap: 12 }}>{users.map((user) => <div key={user.id} style={{ padding: 14, borderRadius: 18, border: "1px solid rgba(255,255,255,.06)", background: "rgba(255,255,255,.03)", display: "grid", gridTemplateColumns: "1fr 220px auto", gap: 12, alignItems: "center" }}><div><div style={{ fontWeight: 800 }}>{user.username || user.first_name || `ID ${user.telegram_id}`}</div><div style={{ fontSize: 12, color: "rgba(161,161,170,.82)", marginTop: 4 }}>telegram_id: {user.telegram_id}</div></div><input value={drafts[user.id] ?? Number(user.balance || 0)} onChange={(e) => setDrafts((prev) => ({ ...prev, [user.id]: e.target.value }))} style={{ height: 44, borderRadius: 12, border: "1px solid rgba(255,255,255,.08)", background: "rgba(255,255,255,.04)", color: "#fff", padding: "0 12px", boxSizing: "border-box" }} /><button onClick={() => onUpdateBalance(user.id, drafts[user.id] ?? Number(user.balance || 0))} style={{ height: 44, padding: "0 14px", borderRadius: 12, border: "1px solid rgba(255,255,255,.08)", background: "linear-gradient(135deg, rgba(99,102,241,.24), rgba(56,189,248,.18))", color: "#fff", cursor: "pointer", fontWeight: 800 }}>РЎРѕС…СЂР°РЅРёС‚СЊ</button></div>)}</div>;
}
function Inbox({ orders, paymentsById, messagesByOrder, selectedOrderId, setSelectedOrderId, onSaveOrder, onSendMessage, onMarkPayment }) {
  const selectedOrder = orders.find((order) => order.id === selectedOrderId) || orders[0] || null;
  const [draftStatus, setDraftStatus] = useState(selectedOrder?.status || "waiting_payment");
  const [draftNotes, setDraftNotes] = useState(selectedOrder?.designer_notes || "");
  const [draftDelivery, setDraftDelivery] = useState(selectedOrder?.delivery_url || "");
  const [messageText, setMessageText] = useState("");

  useEffect(() => {
    setDraftStatus(selectedOrder?.status || "waiting_payment");
    setDraftNotes(selectedOrder?.designer_notes || "");
    setDraftDelivery(selectedOrder?.delivery_url || "");
  }, [selectedOrderId, selectedOrder?.status, selectedOrder?.designer_notes, selectedOrder?.delivery_url]);

  const ordered = useMemo(() => [...orders].sort((a, b) => new Date(b.updated_at || b.created_at) - new Date(a.updated_at || a.created_at)), [orders]);

  return <div style={{ display: "grid", gridTemplateColumns: "360px minmax(0,1fr)", gap: 18 }}><div style={{ ...styles.card, padding: 14, display: "grid", gap: 10, maxHeight: "calc(100vh - 72px)", overflow: "auto" }}>{ordered.map((order) => { const payment = order.payment_id ? paymentsById[order.payment_id] : null; const isInquiry = String(order.service_name || "").toLowerCase().includes("СЂРµРєРІРёР·РёС‚") || String(order.service_name || "").toLowerCase().includes("payment details"); const active = selectedOrder?.id === order.id; return <button key={order.id} onClick={() => setSelectedOrderId(order.id)} style={{ textAlign: "left", padding: 14, borderRadius: 18, border: `1px solid ${active ? "rgba(99,102,241,.32)" : "rgba(255,255,255,.06)"}`, background: active ? "linear-gradient(135deg, rgba(99,102,241,.14), rgba(56,189,248,.08))" : "rgba(255,255,255,.03)", color: "inherit", cursor: "pointer" }}><div style={{ display: "flex", justifyContent: "space-between", gap: 12 }}><div><div style={{ fontSize: 11, color: "rgba(161,161,170,.78)", marginBottom: 5 }}>{order.order_number}</div><div style={{ fontSize: 15, fontWeight: 800 }}>{order.service_name}</div><div style={{ fontSize: 12, color: isInquiry ? "#93c5fd" : "rgba(161,161,170,.82)", marginTop: 6 }}>{isInquiry ? "Р—Р°РїСЂРѕСЃ СЂРµРєРІРёР·РёС‚РѕРІ" : order.users?.username || order.users?.first_name || "РљР»РёРµРЅС‚"}</div></div><div style={{ textAlign: "right" }}><div style={{ fontWeight: 900 }}>{money(order.total_amount)}</div><div style={{ fontSize: 12, color: payment?.status === "paid" ? "#34d399" : "rgba(161,161,170,.82)", marginTop: 6 }}>{payment?.status || order.status}</div></div></div></button>; })}</div><div style={{ ...styles.card, padding: 18, minHeight: 720 }}>{!selectedOrder ? <div style={{ color: "rgba(161,161,170,.82)" }}>Р—Р°РєР°Р·РѕРІ РїРѕРєР° РЅРµС‚.</div> : <div style={{ display: "grid", gap: 16 }}><div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-start", gap: 18 }}><div><div style={{ fontSize: 12, color: "rgba(161,161,170,.78)", marginBottom: 6 }}>{selectedOrder.order_number}</div><div style={{ fontSize: 28, fontWeight: 900, letterSpacing: "-.04em" }}>{selectedOrder.service_name}</div><div style={{ fontSize: 13, color: "rgba(161,161,170,.82)", marginTop: 8 }}>{selectedOrder.users?.username || selectedOrder.users?.first_name || "РљР»РёРµРЅС‚"} В· {money(selectedOrder.total_amount)}</div></div><div style={{ display: "grid", gap: 8, justifyItems: "end" }}><div style={{ padding: "8px 12px", borderRadius: 999, border: "1px solid rgba(255,255,255,.08)", background: "rgba(255,255,255,.05)", fontSize: 12 }}>{selectedOrder.status}</div><div style={{ fontSize: 12, color: "rgba(161,161,170,.8)" }}>{formatDate(selectedOrder.updated_at || selectedOrder.created_at)}</div></div></div><div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 12 }}><div style={{ padding: 14, borderRadius: 18, background: "rgba(255,255,255,.03)", border: "1px solid rgba(255,255,255,.06)" }}><div style={{ fontSize: 11, textTransform: "uppercase", letterSpacing: ".12em", color: "rgba(161,161,170,.82)", marginBottom: 10 }}>Р‘СЂРёС„</div><div style={{ fontSize: 14, lineHeight: 1.6 }}>{selectedOrder.brief || "вЂ”"}</div></div><div style={{ padding: 14, borderRadius: 18, background: "rgba(255,255,255,.03)", border: "1px solid rgba(255,255,255,.06)" }}><div style={{ fontSize: 11, textTransform: "uppercase", letterSpacing: ".12em", color: "rgba(161,161,170,.82)", marginBottom: 10 }}>РџР»Р°С‚РµР¶</div><div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 12 }}><div><div style={{ fontSize: 14, fontWeight: 800 }}>{selectedOrder.payments?.payment_method || "manual"}</div><div style={{ fontSize: 12, color: "rgba(161,161,170,.82)", marginTop: 4 }}>{selectedOrder.payments?.status || "pending"}</div></div>{selectedOrder.payment_id ? <button onClick={() => onMarkPayment(selectedOrder.payment_id, selectedOrder.payments?.status === "paid" ? "pending" : "paid")} style={{ padding: "10px 12px", borderRadius: 12, border: "1px solid rgba(255,255,255,.08)", background: "linear-gradient(135deg, rgba(16,185,129,.18), rgba(5,150,105,.12))", color: "#d1fae5", cursor: "pointer", fontWeight: 800 }}>{selectedOrder.payments?.status === "paid" ? "Р’РµСЂРЅСѓС‚СЊ РІ pending" : "РџРѕРґС‚РІРµСЂРґРёС‚СЊ РѕРїР»Р°С‚Сѓ"}</button> : null}</div></div></div><div style={{ display: "grid", gridTemplateColumns: "240px 1fr", gap: 12 }}><div><div style={{ fontSize: 11, textTransform: "uppercase", letterSpacing: ".12em", color: "rgba(161,161,170,.82)", marginBottom: 8 }}>РЎС‚Р°С‚СѓСЃ</div><select value={draftStatus} onChange={(e) => setDraftStatus(e.target.value)} style={{ width: "100%", height: 48, borderRadius: 14, border: "1px solid rgba(255,255,255,.08)", background: "rgba(255,255,255,.04)", color: "#fff", padding: "0 14px" }}>{ORDER_STATUS_OPTIONS.map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></div><div><div style={{ fontSize: 11, textTransform: "uppercase", letterSpacing: ".12em", color: "rgba(161,161,170,.82)", marginBottom: 8 }}>РЎСЃС‹Р»РєР° РЅР° РІС‹РґР°С‡Сѓ</div><input value={draftDelivery} onChange={(e) => setDraftDelivery(e.target.value)} placeholder="https://..." style={{ width: "100%", height: 48, borderRadius: 14, border: "1px solid rgba(255,255,255,.08)", background: "rgba(255,255,255,.04)", color: "#fff", padding: "0 14px", boxSizing: "border-box" }} /></div></div><div><div style={{ fontSize: 11, textTransform: "uppercase", letterSpacing: ".12em", color: "rgba(161,161,170,.82)", marginBottom: 8 }}>Р—Р°РјРµС‚РєР° РґР»СЏ РєР»РёРµРЅС‚Р°</div><textarea value={draftNotes} onChange={(e) => setDraftNotes(e.target.value)} rows={4} style={{ width: "100%", borderRadius: 18, border: "1px solid rgba(255,255,255,.08)", background: "rgba(255,255,255,.04)", color: "#fff", padding: 14, boxSizing: "border-box", resize: "vertical" }} /></div><div style={{ display: "flex", gap: 10 }}><button onClick={() => onSaveOrder(selectedOrder.id, { status: draftStatus, designerNotes: draftNotes, deliveryUrl: draftDelivery })} style={{ padding: "12px 16px", borderRadius: 14, border: "1px solid rgba(255,255,255,.08)", background: "linear-gradient(135deg, #6366f1, #8b5cf6)", color: "#fff", fontWeight: 900, cursor: "pointer" }}>РЎРѕС…СЂР°РЅРёС‚СЊ РєР°СЂС‚РѕС‡РєСѓ</button></div><div style={{ borderTop: "1px solid rgba(255,255,255,.06)", paddingTop: 16 }}><div style={{ fontSize: 16, fontWeight: 900, marginBottom: 12 }}>Р§Р°С‚ Р·Р°РєР°Р·Р°</div><div style={{ display: "grid", gap: 10, maxHeight: 320, overflow: "auto", paddingRight: 4 }}>{(messagesByOrder[selectedOrder.id] || []).map((message) => <div key={message.id} style={{ padding: 12, borderRadius: 16, background: message.sender_role === "designer" ? "rgba(99,102,241,.14)" : "rgba(255,255,255,.04)", border: `1px solid ${message.sender_role === "designer" ? "rgba(99,102,241,.24)" : "rgba(255,255,255,.06)"}` }}><div style={{ fontSize: 12, color: "rgba(161,161,170,.78)", marginBottom: 6 }}>{message.sender_role === "designer" ? "РўС‹" : selectedOrder.users?.username || selectedOrder.users?.first_name || "РљР»РёРµРЅС‚"} В· {formatDate(message.created_at)}</div><div style={{ lineHeight: 1.6 }}>{message.text}</div></div>)}</div><div style={{ display: "grid", gridTemplateColumns: "1fr auto", gap: 10, marginTop: 12 }}><textarea value={messageText} onChange={(e) => setMessageText(e.target.value)} rows={3} placeholder="РћС‚РІРµС‚ РєР»РёРµРЅС‚Сѓ..." style={{ borderRadius: 16, border: "1px solid rgba(255,255,255,.08)", background: "rgba(255,255,255,.04)", color: "#fff", padding: 14, resize: "vertical" }} /><button onClick={() => { onSendMessage(selectedOrder.id, messageText); setMessageText(""); }} style={{ alignSelf: "end", height: 48, padding: "0 16px", borderRadius: 14, border: "1px solid rgba(255,255,255,.08)", background: "linear-gradient(135deg, rgba(56,189,248,.28), rgba(99,102,241,.22))", color: "#fff", fontWeight: 900, cursor: "pointer" }}>РћС‚РїСЂР°РІРёС‚СЊ</button></div></div></div>}</div></div>;
}

function ContentManager({ sectionKey, contentDraft, setContentDraft, onSaveAll, dirty }) {
  const config = contentConfig[sectionKey];
  const items = getPath(contentDraft, config.path) || [];
  const [selectedIndex, setSelectedIndex] = useState(0);
  const [editorValue, setEditorValue] = useState(JSON.stringify(items[0] || config.template, null, 2));
  useEffect(() => { const safeIndex = Math.min(selectedIndex, Math.max(items.length - 1, 0)); setSelectedIndex(safeIndex); setEditorValue(JSON.stringify(items[safeIndex] || config.template, null, 2)); }, [sectionKey, items.length]);
  const applyEditor = () => { const nextItem = JSON.parse(editorValue); const nextItems = [...items]; if (nextItems.length === 0) nextItems.push(nextItem); else nextItems[selectedIndex] = nextItem; setContentDraft((prev) => setPath(prev, config.path, nextItems)); };
  const addItem = () => { const nextItems = [...items, structuredClone(config.template)]; setContentDraft((prev) => setPath(prev, config.path, nextItems)); setSelectedIndex(nextItems.length - 1); setEditorValue(JSON.stringify(nextItems[nextItems.length - 1], null, 2)); };
  const duplicateItem = () => { const current = items[selectedIndex]; if (!current) return; const copy = structuredClone(current); if (copy.id) copy.id = `${copy.id}_copy_${Date.now()}`; const nextItems = [...items]; nextItems.splice(selectedIndex + 1, 0, copy); setContentDraft((prev) => setPath(prev, config.path, nextItems)); setSelectedIndex(selectedIndex + 1); setEditorValue(JSON.stringify(copy, null, 2)); };
  const removeItem = () => { if (!items[selectedIndex]) return; const nextItems = items.filter((_, index) => index !== selectedIndex); setContentDraft((prev) => setPath(prev, config.path, nextItems)); const nextIndex = Math.max(selectedIndex - 1, 0); setSelectedIndex(nextIndex); setEditorValue(JSON.stringify(nextItems[nextIndex] || config.template, null, 2)); };
  return <div style={{ display: "grid", gridTemplateColumns: "320px minmax(0,1fr)", gap: 18 }}><div style={{ ...styles.card, padding: 14, display: "grid", gap: 10, maxHeight: "calc(100vh - 72px)", overflow: "auto" }}><div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", gap: 10, padding: "4px 4px 10px" }}><div><div style={{ fontSize: 18, fontWeight: 900 }}>{config.title}</div><div style={{ fontSize: 12, color: "rgba(161,161,170,.82)" }}>{items.length} Р·Р°РїРёСЃРµР№</div></div><button onClick={addItem} style={{ height: 40, padding: "0 12px", borderRadius: 12, border: "1px solid rgba(255,255,255,.08)", background: "linear-gradient(135deg, rgba(99,102,241,.24), rgba(56,189,248,.18))", color: "#fff", cursor: "pointer", fontWeight: 900 }}>Р”РѕР±Р°РІРёС‚СЊ</button></div>{items.map((item, index) => { const active = index === selectedIndex; return <button key={`${config.title}_${index}`} onClick={() => { setSelectedIndex(index); setEditorValue(JSON.stringify(item, null, 2)); }} style={{ textAlign: "left", padding: 14, borderRadius: 16, border: `1px solid ${active ? "rgba(99,102,241,.32)" : "rgba(255,255,255,.06)"}`, background: active ? "linear-gradient(135deg, rgba(99,102,241,.14), rgba(56,189,248,.08))" : "rgba(255,255,255,.03)", color: "inherit", cursor: "pointer" }}><div style={{ fontWeight: 800 }}>{config.getLabel(item)}</div><div style={{ fontSize: 12, color: "rgba(161,161,170,.82)", marginTop: 5 }}>{config.getMeta(item)}</div></button>; })}</div><div style={{ ...styles.card, padding: 18, display: "grid", gap: 14 }}><div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", gap: 10 }}><div><div style={{ fontSize: 24, fontWeight: 900 }}>{config.title}</div><div style={{ fontSize: 12, color: "rgba(161,161,170,.82)", marginTop: 4 }}>{dirty ? "Р•СЃС‚СЊ РЅРµСЃРѕС…СЂР°РЅРµРЅРЅС‹Рµ РёР·РјРµРЅРµРЅРёСЏ" : "Р’СЃРµ СЃРёРЅС…СЂРѕРЅРёР·РёСЂРѕРІР°РЅРѕ"}</div></div><div style={{ display: "flex", gap: 8 }}><button onClick={duplicateItem} style={{ height: 42, padding: "0 12px", borderRadius: 12, border: "1px solid rgba(255,255,255,.08)", background: "rgba(255,255,255,.05)", color: "#fff", cursor: "pointer" }}>Р”СѓР±Р»СЊ</button><button onClick={removeItem} style={{ height: 42, padding: "0 12px", borderRadius: 12, border: "1px solid rgba(248,113,113,.18)", background: "rgba(248,113,113,.08)", color: "#fecaca", cursor: "pointer" }}>РЈРґР°Р»РёС‚СЊ</button><button onClick={applyEditor} style={{ height: 42, padding: "0 14px", borderRadius: 12, border: "1px solid rgba(255,255,255,.08)", background: "linear-gradient(135deg, rgba(99,102,241,.24), rgba(56,189,248,.18))", color: "#fff", cursor: "pointer", fontWeight: 900 }}>РџСЂРёРјРµРЅРёС‚СЊ JSON</button><button onClick={onSaveAll} style={{ height: 42, padding: "0 14px", borderRadius: 12, border: "1px solid rgba(16,185,129,.22)", background: "linear-gradient(135deg, rgba(16,185,129,.22), rgba(5,150,105,.18))", color: "#ecfdf5", cursor: "pointer", fontWeight: 900 }}>РЎРѕС…СЂР°РЅРёС‚СЊ РІ CMS</button></div></div><textarea value={editorValue} onChange={(e) => setEditorValue(e.target.value)} spellCheck={false} style={{ minHeight: 540, width: "100%", borderRadius: 22, border: "1px solid rgba(255,255,255,.08)", background: "rgba(2,4,9,.72)", color: "#f8fafc", padding: 18, boxSizing: "border-box", resize: "vertical", fontFamily: "ui-monospace, SFMono-Regular, Consolas, monospace", fontSize: 13, lineHeight: 1.6 }} /></div></div>;
}
export default function AdminApp() {
  const tg = window.Telegram?.WebApp;
  const [devId, setDevId] = useState(() => localStorage.getItem("rs_admin_dev_id") || LOCAL_ADMIN_ID);
  const telegramUser = useMemo(() => buildTelegramUser(tg?.initDataUnsafe?.user, devId), [tg, devId]);
  const [section, setSection] = useState("dashboard");
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState("");
  const [metrics, setMetrics] = useState({ activeOrders: 0, unpaidOrders: 0, revenue: 0, users: 0, inbox: 0 });
  const [users, setUsers] = useState([]);
  const [orders, setOrders] = useState([]);
  const [payments, setPayments] = useState([]);
  const [messages, setMessages] = useState([]);
  const [contentDraft, setContentDraft] = useState(null);
  const [dirtyContent, setDirtyContent] = useState(false);
  const [selectedOrderId, setSelectedOrderId] = useState(null);
  const [setupInfo, setSetupInfo] = useState(null);

  useEffect(() => { tg?.ready?.(); tg?.expand?.(); }, [tg]);

  const load = async () => {
    if (!telegramUser?.id) { setLoading(false); return; }
    setLoading(true); setError("");
    try {
      const result = await adminPost("/api/admin/bootstrap", {}, telegramUser);
      setMetrics(result.metrics || {});
      setUsers(result.users || []);
      setOrders(result.orders || []);
      setPayments(result.payments || []);
      setMessages(result.messages || []);
      setContentDraft(result.content || null);
      setSelectedOrderId((result.orders || [])[0]?.id || null);
      setSetupInfo(result.setup || null);
      setDirtyContent(false);
    } catch (err) {
      const message = err.message || "Не удалось загрузить админку";
      const isLocalMode = !tg?.initDataUnsafe?.user;
      if (isLocalMode && message.includes("Admin access denied") && devId !== LOCAL_ADMIN_ID) {
        localStorage.setItem("rs_admin_dev_id", LOCAL_ADMIN_ID);
        setDevId(LOCAL_ADMIN_ID);
        return;
      }
      setError(message);
    } finally {
      setLoading(false);
    }
  };

  useEffect(() => { load(); }, [telegramUser?.id]);

  const paymentsById = useMemo(() => Object.fromEntries(payments.map((item) => [item.id, item])), [payments]);
  const messagesByOrder = useMemo(() => messages.reduce((acc, item) => { if (!acc[item.order_id]) acc[item.order_id] = []; acc[item.order_id].push(item); return acc; }, {}), [messages]);

  const handleSaveOrder = async (orderId, payload) => { await adminPost("/api/admin/order/update", { orderId, ...payload }, telegramUser); await load(); };
  const handleSendMessage = async (orderId, text) => { if (!String(text || "").trim()) return; await adminPost("/api/admin/message/send", { orderId, text }, telegramUser); await load(); };
  const handlePaymentUpdate = async (paymentId, status) => { await adminPost("/api/admin/payment/update", { paymentId, status }, telegramUser); await load(); };
  const handleBalanceUpdate = async (userId, balance) => { await adminPost("/api/admin/user/update-balance", { userId, balance }, telegramUser); await load(); };
  const handleSaveContent = async () => { await adminPost("/api/admin/content/save", { content: contentDraft }, telegramUser); await load(); };

  if (!telegramUser?.id) {
    return <div style={{ ...styles.bg, display: "grid", placeItems: "center", padding: 24 }}><div style={{ ...styles.card, width: "min(520px, 100%)", padding: 28 }}><div style={{ fontSize: 28, fontWeight: 900, marginBottom: 10 }}>Rival Admin</div><div style={{ fontSize: 14, color: "rgba(161,161,170,.84)", lineHeight: 1.7, marginBottom: 18 }}>РћС‚РєСЂРѕР№ Р°РґРјРёРЅРєСѓ РІРЅСѓС‚СЂРё Telegram Mini App РёР»Рё РІРІРµРґРё СЃРІРѕР№ Telegram ID РґР»СЏ Р»РѕРєР°Р»СЊРЅРѕР№ РїСЂРѕРІРµСЂРєРё РІ Р±СЂР°СѓР·РµСЂРµ.</div><input value={devId} onChange={(e) => { setDevId(e.target.value); localStorage.setItem("rs_admin_dev_id", e.target.value); }} placeholder="Telegram ID" style={{ width: "100%", height: 52, borderRadius: 16, border: "1px solid rgba(255,255,255,.08)", background: "rgba(255,255,255,.04)", color: "#fff", padding: "0 14px", boxSizing: "border-box" }} /><button onClick={() => { setDevId(LOCAL_ADMIN_ID); localStorage.setItem("rs_admin_dev_id", LOCAL_ADMIN_ID); }} style={{ marginTop: 12, width: "100%", height: 48, borderRadius: 14, border: "1px solid rgba(99,102,241,.28)", background: "linear-gradient(135deg, rgba(99,102,241,.22), rgba(56,189,248,.16))", color: "#fff", cursor: "pointer", fontWeight: 800 }}>Войти как local admin</button><button onClick={() => { setDevId(LOCAL_ADMIN_ID); localStorage.setItem("rs_admin_dev_id", LOCAL_ADMIN_ID); }} style={{ marginTop: 12, width: "100%", height: 48, borderRadius: 14, border: "1px solid rgba(99,102,241,.28)", background: "linear-gradient(135deg, rgba(99,102,241,.22), rgba(56,189,248,.16))", color: "#fff", cursor: "pointer", fontWeight: 800 }}>????? ??? local admin</button></div></div>;
  }

  const contentSections = ["portfolio", "courses", "services", "reviews", "faq", "home", "socials"];
  const isContentSection = contentSections.includes(section);
  const dataSections = ["dashboard", "inbox", "payments", "clients"];

  return <div style={styles.bg}><div style={styles.shell}><Sidebar section={section} setSection={setSection} dirtyContent={dirtyContent} /><main style={{ display: "grid", gap: 18 }}><div style={{ ...styles.card, padding: "18px 22px", display: "flex", alignItems: "center", justifyContent: "space-between", gap: 18 }}><div><div style={{ fontSize: 30, fontWeight: 900, letterSpacing: "-.05em" }}>{NAV_ITEMS.find((item) => item.id === section)?.label || "Admin"}</div><div style={{ fontSize: 13, color: "rgba(161,161,170,.82)", marginTop: 6 }}>{telegramUser.username ? `@${telegramUser.username}` : telegramUser.first_name || "РђРґРјРёРЅ"} В· РѕС‚РґРµР»СЊРЅРѕРµ РїСЂРёР»РѕР¶РµРЅРёРµ СѓРїСЂР°РІР»РµРЅРёСЏ Rival Space</div></div><div style={{ display: "flex", gap: 10 }}><button onClick={load} style={{ height: 42, padding: "0 14px", borderRadius: 12, border: "1px solid rgba(255,255,255,.08)", background: "rgba(255,255,255,.05)", color: "#fff", cursor: "pointer" }}>РћР±РЅРѕРІРёС‚СЊ</button>{contentDraft && isContentSection ? <button onClick={handleSaveContent} style={{ height: 42, padding: "0 14px", borderRadius: 12, border: "1px solid rgba(16,185,129,.22)", background: "linear-gradient(135deg, rgba(16,185,129,.22), rgba(5,150,105,.18))", color: "#ecfdf5", cursor: "pointer", fontWeight: 900 }}>РЎРѕС…СЂР°РЅРёС‚СЊ CMS</button> : null}</div></div>{setupInfo?.requiresServiceKey ? <div style={{ ...styles.card, padding: 18, borderColor: "rgba(251,191,36,.22)", background: "linear-gradient(180deg, rgba(46,33,7,.9) 0%, rgba(20,13,5,.96) 100%)" }}><div style={{ display: "flex", alignItems: "flex-start", gap: 12 }}><div style={{ width: 42, height: 42, borderRadius: 14, display: "grid", placeItems: "center", background: "rgba(251,191,36,.14)", border: "1px solid rgba(251,191,36,.18)" }}><SystemIcon name="spark" size={18} color="#fde68a" animated /></div><div><div style={{ fontSize: 18, fontWeight: 900, color: "#fef3c7", marginBottom: 6 }}>Р”Р»СЏ live inbox РЅСѓР¶РµРЅ secret key</div><div style={{ fontSize: 13, lineHeight: 1.7, color: "rgba(254,243,199,.82)" }}>{setupInfo.message}</div><div style={{ fontSize: 12, lineHeight: 1.7, color: "rgba(254,243,199,.66)", marginTop: 8 }}>.env в†’ SUPABASE_SECRET_KEY РёР»Рё SUPABASE_SERVICE_KEY. РџРѕСЃР»Рµ СЌС‚РѕРіРѕ РІ Р°РґРјРёРЅРєСѓ РЅР°С‡РЅСѓС‚ РїСЂРёР»РµС‚Р°С‚СЊ Р·Р°РєР°Р·С‹, СЂРµРєРІРёР·РёС‚С‹, С‡Р°С‚ Рё РїР»Р°С‚РµР¶Рё РёР· РѕР±С‹С‡РЅРѕРіРѕ РїСЂРёР»РѕР¶РµРЅРёСЏ.</div></div></div></div> : null}{error ? <div style={{ ...styles.card, padding: 18, color: "#fecaca", borderColor: "rgba(248,113,113,.22)" }}>{error}</div> : null}{loading ? <div style={{ ...styles.card, padding: 28, color: "rgba(161,161,170,.82)" }}>Р—Р°РіСЂСѓР¶Р°СЋ Р°РґРјРёРЅ-СЃРёСЃС‚РµРјСѓвЂ¦</div> : null}{!loading && section === "dashboard" ? <Dashboard metrics={metrics} orders={orders} payments={payments} /> : null}{!loading && section === "inbox" ? <Inbox orders={orders} paymentsById={paymentsById} messagesByOrder={messagesByOrder} selectedOrderId={selectedOrderId} setSelectedOrderId={setSelectedOrderId} onSaveOrder={handleSaveOrder} onSendMessage={handleSendMessage} onMarkPayment={handlePaymentUpdate} /> : null}{!loading && section === "payments" ? <Payments payments={payments} onUpdate={handlePaymentUpdate} onOpenOrder={(paymentId) => { const order = orders.find((item) => item.payment_id === paymentId); if (order) { setSelectedOrderId(order.id); setSection("inbox"); } }} /> : null}{!loading && section === "clients" ? <Clients users={users} onUpdateBalance={handleBalanceUpdate} /> : null}{!loading && contentDraft && isContentSection ? <ContentManager sectionKey={section} contentDraft={contentDraft} setContentDraft={(updater) => { setContentDraft((prev) => { const next = typeof updater === "function" ? updater(prev) : updater; setDirtyContent(true); return next; }); }} onSaveAll={handleSaveContent} dirty={dirtyContent} /> : null}{!loading && setupInfo?.requiresServiceKey && dataSections.includes(section) && !orders.length && !payments.length && !users.length ? <div style={{ ...styles.card, padding: 22, color: "rgba(161,161,170,.9)", lineHeight: 1.8 }}>Р­С‚Р° С‡Р°СЃС‚СЊ СѓР¶Рµ РіРѕС‚РѕРІР°, РЅРѕ Р¶РґРµС‚ РїРѕРґРєР»СЋС‡РµРЅРёРµ СЃРµСЂРІРµСЂРЅРѕРіРѕ РґРѕСЃС‚СѓРїР° Рє Supabase. РљР°Рє С‚РѕР»СЊРєРѕ РІ <code>SUPABASE_SECRET_KEY</code> РёР»Рё <code>SUPABASE_SERVICE_KEY</code> РїРѕСЏРІРёС‚СЃСЏ СЃРµСЂРІРµСЂРЅС‹Р№ РєР»СЋС‡ Supabase, СЃСЋРґР° РЅР°С‡РЅСѓС‚ РїСЂРёС…РѕРґРёС‚СЊ РІС…РѕРґСЏС‰РёРµ Р·Р°РїСЂРѕСЃС‹ СЂРµРєРІРёР·РёС‚РѕРІ, Р·Р°РєР°Р·С‹, РїР»Р°С‚РµР¶Рё Рё СЃРѕРѕР±С‰РµРЅРёСЏ РёР· РѕР±С‹С‡РЅРѕРіРѕ РїСЂРёР»РѕР¶РµРЅРёСЏ.</div> : null}</main></div></div>;
}

